# strut-issue
  struct Issue {
        EnumerableSet.AddressSet voters;
        string issueDesc;
        uint256 CallFor;
        uint256 CallsAgainst;
            bool passed;
        bool closed;
      }
